---
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

const html = readFileSync(
  join(process.cwd(), 'public', 'uvb.edu.mx', 'index.html'),
  'utf-8',
);
---

<Fragment set:html={html} />
