---
import { join } from 'node:path';
import { readMirrorHtml } from '../lib/mirror-pages';

const html = readMirrorHtml(
  join(process.cwd(), 'src', 'mirror', 'uvb.edu.mx', 'index.html'),
);
---

<Fragment set:html={html} />
